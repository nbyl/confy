management:
    server:
        servlet:
            context-path: /management

spring:
    application:
        name: confy

    data:
        rest:
            basePath: /api

    jackson:
        serialization.write_dates_as_timestamps: false

    jpa:
        hibernate:
            ddl-auto: none
    security:
        user:
            password: test1234

